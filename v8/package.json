{
  "name": "openfield-football-v8",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Openfield Football V8 - TypeScript + Phaser 4 rewrite of the tested V7 prototype",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
